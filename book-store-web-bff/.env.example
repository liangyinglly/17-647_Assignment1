PORT=80

# Option 1: one shared backend base URL (e.g., internal LB)
URL_BASE_BACKEND_SERVICES=

# Option 2: per-service URLs (overrides shared base when set)
CUSTOMER_SERVICE_URL=http://customer-service:3000
BOOK_SERVICE_URL=http://book-service:3000
